{
  "name": "@astra/components",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./index.ts",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build": "vite build"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.30.1"
  }
}
